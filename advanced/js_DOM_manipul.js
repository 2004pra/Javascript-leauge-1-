// JS DOM MANIPULATION

// the window object--> the main page of any website is the window object itself its the global object which has the highest heirarchy 
//every global vaiable and global method the behaviour of the window object is defined in the browser
//whatever the website you are at is like a window itself

//our browser create this window object 

//DOM --> Document Object Model
//lets say you have a file name index.html if you are converting that file inside a javascript object then it is called DOM

  //BOM--> Browser Object Model
    //it is used to interact with the browser
    //leaving the html content it is used to interact with the browser
     

    